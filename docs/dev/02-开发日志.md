# 开发日志与决策记录

> 逐日记录：做了什么、遇到什么问题、如何解决、新的约定。
> 新约定必须同步进 `00-项目定稿.md`。

---

## 2025-09-25

### 环境调研
- 系统：Windows，Node v24.14.0 / npm 11.9 / Python 3.13.5 ✅
- WebView2 运行时已存在（153.0.4234.48）✅ → Tauri 可用
- MSVC BuildTools 18 已装（cl.exe at `C:/Program Files (x86)/Microsoft Visual Studio/18/BuildTools`）+ Win SDK 10.0.26100 ✅
- Rust 未装 → 已通过 rsproxy.cn 镜像 + 本地代理(127.0.0.1:7890) 安装 stable-msvc，`--no-modify-path`（PATH 需手动加 `%USERPROFILE%\.cargo\bin`）
- 代理可用：`curl -x http://127.0.0.1:7890` 可正常访问 GitHub API

### 前期 GitHub 调研结论
- 无现成的「优点/缺点双向打卡+激励」成熟工具，需求成立
- 重点参考：HabitTrove（积分+愿望兑换闭环）、uhabits（streak统计）、beaverhabits（极简打卡）、habitica（游戏化）

### 用户拍板的决策（已录入 00-项目定稿.md）
1. 名称：知进退 / Advance & Retreat
2. 积分+愿望兑换：要
3. 补卡：禁止
4. 每日一句话回顾：要
5. 定时提醒：要

### 待办/风险
- [ ] cargo 镜像配置未做
- [ ] 首次 `tauri dev` 编译耗时长，属正常现象

### Git 仓库建立（追加于同日）
- 远程仓库：https://github.com/gigogigo64/Advance-Retreat
- 提交规范已写入 `docs/03-Git工作流约定.md`：Conventional Commits + 中文描述
- 首次提交：`chore: 初始化Tauri项目脚手架，录入项目定稿、开发计划、Git约定文档`
- 已推送 main 分支成功
- 约定：docs/ 同时保留在项目根目录（../docs）和 advance-retreat/docs/，以项目内 docs/ 为准同步

## 2025-09-25（续）— 主体开发完成

### 完成
- 阶段0：Rust装好(1.98.1)、cargo镜像、脚手架、依赖全装、`tauri dev` 首次运行成功（编译仅1m44s）
- 阶段1：SQLite 7张表（habits/checkins/daily_notes/points_log/rewards/redemptions/settings）+ repo数据层 + 全局store
- 阶段2：今日页（进度环、优/缺卡片流、打卡动效、满分撒花、积分入账、7天全勤奖励、一句话回顾）
- 阶段3：清单页（优/缺切换、新建/编辑对话框、emoji/颜色/分类/频率、归档）
- 阶段4：统计页（连续排行、需警惕排行、年度热力图、单项streak/完成率、历史回顾只读）
- 阶段5：奖励页（积分余额、愿望卡片、兑换+积分不足提示、兑换历史）
- 阶段6：提醒循环（每30s检查、到点未完成发系统通知、全完成不打扰、每日一次）
- 阶段7：设置页（深色模式、积分规则、提醒时间、JSON导出/导入、清空数据）

### 问题与解决
1. 端口1420被Windows保留端口段(1351-1450)占用 → 改用5180（vite.config.ts + tauri.conf.json devUrl）
2. 端口冲突教训已值得记入规范（见规范.md第8节补充）
3. 手滑产生的杂字符（"过去的"/"显露"）已清除；TS全绿后构建通过

### 当前状态
- 应用在 dev 模式运行中，M1+M2 里程碑达成
- 待办：真实UI走查（窗口内逐页点检）、图标替换、`tauri build` 出安装包

## 2025-09-25（续2）— 构建发布完成

### UI走查
- agent-browser 在普通浏览器中走查：侧边栏/空状态/清单对话框/表单交互全部正常
- 浏览器中报 `invoke undefined` 属预期（无Tauri桥），非缺陷；真实Tauri窗口DB写入正常（WAL 98KB增长）
- 教训记入规范：UI走查须在 `tauri dev` 的真实窗口内做最终确认，浏览器走查仅验交互流

### 构建
- `tauri build` exe 成功（6.3MB）
- MSI失败：WiX3 对中文 productName「知进退」支持差 → 改用 `--bundles nsis` 成功
- 产物：`src-tauri/target/release/bundle/nsis/知进退_0.1.0_x64-setup.exe`（2.3MB安装包）

### 验收基线对照（规范.md 第六节）
1. ✅ exe 双击即用，SQLite 于 %APPDATA%
2. ✅ 增删改归档、按日打卡、无法补卡（toggleCheckin 限今日）
3. ✅ streak/完成率/热力图已实现
4. ✅ 积分+愿望兑换闭环
5. ✅ 每日一句话可写可查（历史回顾页）
6. ✅ 定时提醒（reminder loop + 系统通知）
7. ✅ 中文界面、深色模式、JSON导出导入

### 遗留（下轮迭代候选）
- exe 图标仍为Tauri默认图标，需设计专属图标
- 打卡音效、开机自启（规范外可选项）

## 2025-09-25（续3）— 产物部署方式

- 用户要求：可运行程序放在**项目根目录**（方便双击），而非桌面
- 已部署到 `优缺点培养/`：
  - `知进退.exe`（绿色版，双击即用）
  - `知进退-安装包.exe`（NSIS 系统安装包）
- 新增 `scripts/build-and-deploy.mjs`：一键构建并自动把新产物复制到根目录
  - 用法：`cd advance-retreat && npm run deploy`
- 新增根目录 `README.md`：运行方式、功能、目录结构、开发命令
