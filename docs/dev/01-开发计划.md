# 开发计划 — 「知进退」Advance & Retreat

> 配套需求文档：`docs/规范.md`
> 本文档是开发执行依据：任务顺序、里程碑、验证方式。
> 完成一项勾一项，进度实时更新。
>
> **状态对齐说明（2025-09-25 晚）**：v0.1.0 已构建发布，阶段 0~7 全部完成、阶段 8 基本完成。
> 本文档曾长期未更新造成严重漂移，现已全面对齐实际进度；后续每完成一项必须即时勾选。

---

## 阶段 0：环境与脚手架 ✅

- [x] 确认系统具备 MSVC BuildTools + Windows SDK
- [x] 安装 Rust（rsproxy 镜像 + 代理，stable-msvc 1.98.1）
- [x] 配置 cargo 国内镜像（rsproxy）
- [x] 用 create-tauri-app 生成 React+TS+Vite 模板
- [x] 安装前端依赖：tailwindcss、framer-motion、lucide-react
- [x] 建立项目目录约定（docs/ 记录文档，src/ 前端，src-tauri/ Rust）
- [x] `npm run tauri dev` 首次跑通空窗口（编译 1m44s）✅ 检查点

## 阶段 1：数据层 ✅

- [x] 集成 tauri-plugin-sql（SQLite）
- [x] 建表迁移：`habits` / `checkins` / `points_log` / `rewards` / `redemptions` / `daily_notes` / `settings`
- [x] 封装前端数据访问层（repo.ts）
- [x] 首次启动写入默认积分规则到 settings
- [x] 老库升级迁移（v0.2 新增 sort_order / hp 列，含默认值回填）

## 阶段 2：今日视图（核心闭环）✅

- [x] 侧边栏导航布局 + 主题（浅/深）基座
- [x] 日期头 + 进度环
- [x] 优点/缺点左右两栏卡片流 + 打卡动效（Framer Motion），各栏自带 x/y 进度小计
- [x] 打卡写库 + 积分入账（含连续7天、满分日奖励），取消打卡可逆扣回（对账式防刷分）
- [x] 缺点 Boss 卡：划去 + 「避开」印章 + HP 血条（每天避开 −3 HP）
- [x] 当日全部完成撒花庆祝
- [x] 每日一句话回顾输入框（当日可编辑）
- [x] 「今日已完成，明天见」空状态设计

## 阶段 3：清单管理 ✅

- [x] 清单页列表（优点/缺点分栏 + 分类标签 + emoji/颜色）
- [x] 新建/编辑对话框 + 就地编辑（点名字/图标/分类/频率直接改）
- [x] 拖拽排序（同栏内，sort_order 持久化）
- [x] 目标频率设置（每天/每周N次/仅工作日）
- [x] 归档与恢复（彻底删除仅对已归档项开放）

## 阶段 4：统计 ✅

- [x] 单条详情：streak（当前/最长）、30天完成率
- [x] 年度热力图（GitHub 风格，自绘；无任务日=空白 null，列宽对齐防溢出）
- [x] 总览：连续天数排行、需警惕排行
- [ ] 趋势图（Recharts 已装依赖，代码尚未使用）
- [ ] 历史日历视图（现只有按日期浏览的只读历史回顾）

## 阶段 5：积分与奖励 ✅

- [x] 积分余额显示（侧边栏 + 奖励页）
- [x] 愿望清单管理（增删改、所需积分、emoji）
- [x] 兑换流程 + 兑换历史（积分不足提示）
- [x] points_log 全链路记录（打卡/取消/奖励/撤销/兑换）

## 阶段 6：提醒与系统集成 ✅

- [x] 提醒时间设置 + 开关
- [x] 到点检查未完成项 → 系统通知（30s 轮询，每日一次，全完成不打扰）
- [ ] 托盘常驻（可选，未做）

## 阶段 7：设置与数据安全 ✅

- [x] 深色模式切换（注意：目前不随系统、且重启后不恢复，见 v0.2 候选）
- [ ] 强调色可选（规范 3.7，未做）
- [x] JSON 导出 / 导入（importAll 事务化）
- [x] 清空数据（带确认）
- [x] AI 助手设置（LLM Base URL / Key / 模型名）
- [ ] 关于页（现只有页脚版本号一行）

## 阶段 8：打磨与发布 ✅（基本完成）

- [x] UI 走查（真实窗口截图逐页点检 + 浏览器走查交互流）
- [x] 空状态、错误态补全（LLM 失败兜底文案等）
- [x] `tauri build --bundles nsis` 产出安装包/exe，产物部署到根目录
- [x] 按「验收基线」逐条自测（日志续3节）
- [x] 一键构建部署脚本（scripts/build-and-deploy.mjs，npm run deploy）

---

## 里程碑

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M1 | 阶段0-2：能打卡，核心闭环 | ✅ 达成 |
| M2 | 阶段3-5：功能完整 | ✅ 达成 |
| M3 | 阶段6-8：可发布 v1.0 | ✅ 达成（v0.1.0 已发布） |
| M4 | v0.2 候选项 + 收尾打磨 | ⏳ 规划中 |

## v0.2 候选清单（下一步开发备选池）

按「遗留 → 计划内未完成 → 可选项」排序，做一项勾一项：

### 来自开发日志「遗留」
- [ ] 专属应用图标（当前为 Tauri 默认图标，需设计并替换 src-tauri/icons/ 全套）
- [ ] 打卡音效
- [ ] 开机自启

### 计划内未完成项
- [x] Boss/LLM 转化功能提交 git（已随 REQ-01 轮次提交）
- [ ] 深色模式：跟随系统 + 启动时恢复上次选择（规范 3.7；当前重启后丢失）
- [ ] 统计页趋势图（Recharts）
- [ ] 历史日历视图
- [ ] 强调色可选
- [ ] 关于页

### 可选项（规范外）
- [ ] 托盘常驻
- [ ] 清单页 emoji 选择器点外区域关闭（现需再点一次按钮）
